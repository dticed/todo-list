const todoList = () => {
    const commands = ["create", "delete"];
    const list = [];

    const getCommands = () => {
        commands.forEach(command => {
            return command;
        })
    }

    const getList = () => {
        list.forEach(item => {
            return item;
        });
    }
}